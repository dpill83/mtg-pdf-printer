# Legacy

Archived code that is no longer imported or used by the app.

- **ThemeContext.js** — Dark/light theme scaffold. Was wired in `index.js` but `useTheme` was never consumed and no styles referenced the theme classes.
